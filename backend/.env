# Copy this file to .env and fill in real values. NEVER commit the real .env file.

# PostgreSQL connection string
DATABASE_URL="postgresql://lunch_user:Hypex365LOSmaaznawazdev@db:5432/lunch_ordering?schema=public"

# JWT secrets - generate strong random values, e.g. `openssl rand -base64 48`
JWT_ACCESS_SECRET="+7LEjG5ghCugLZ4jpNDw20WboaV+88SEcJWKRW1c3atR4fzoziQhwunbuBDh9dQP"
JWT_REFRESH_SECRET="Haa7cgVNEpoYRmwqcrSfwSKulmJsmPQdhMZcREYXBEORK3qX29i1GRW6ovrq+Y1T"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# App
PORT=4000
NODE_ENV="development"

# Allowed frontend origin(s) for CORS - comma separated, no trailing slash
CORS_ORIGIN="https://lunch.hypemediaonline.org"

# Cookie security
COOKIE_SECURE="true"
# set COOKIE_SECURE=true in production (requires HTTPS)

# Fixed lunch price fallback (Rs.)
DEFAULT_LUNCH_PRICE=275.00

# Self-registration - only emails ending in this domain can register.
# Leave blank only for local testing; ALWAYS set this in production.
ALLOWED_EMAIL_DOMAIN=""

#Order Cutoff time
ORDER_CUTOFF_TIME="09:30"

# Used to build the verification link sent by email
FRONTEND_URL="https://lunch.hypemediaonline.org"

# SMTP settings for sending verification emails.
# If left unset, emails are printed to the server console instead (dev only).
SMTP_HOST="mail.365news.pk"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="test@365news.pk"
SMTP_PASS="Test@@++##"
SMTP_FROM="test@365news.pk"
